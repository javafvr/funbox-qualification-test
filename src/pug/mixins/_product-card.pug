mixin product-card(title, weight, descr, state)
	if state
		article.product-card(data-descr= descr role="article")
			.product-card__content
				h5.title-5 Сказочное заморское яство
				h3.title-3 Нямушка
				h4.title-4 #{title}
				.product-card__content__descr 			
					if block 
						block
					else
						10 порций
			.bage.text-center 
				p.bage__text #{weight}
				p.bage__text--small кг
		p.footer.text-center(role="contentinfo") Чего сидишь? Порадуй котэ, 
			a.buy купи.
	else
		article.product-card.product-card--disabled
			.product-card__overlay
			.product-card__content.product-card__content--disable
				h5.title-5 Сказочное заморское яство
				h3.title-3 Нямушка
				h4.title-4 #{title}
				.product-card__content__descr
					if block 
						block
					else
						10 порций
			.bage.text-center 
				p.bage__text #{weight}
				p.bage__text--small кг
		p.footer.footer--disabled.text-center Печалька, с курой закончился.

